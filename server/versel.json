{
  "versions": 2,
  "builds": [
    {
      "src": "src/index.js",
      "use": "@vercel/static",
      "config": {
        "includeFiles": ["dist/**"]
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/index.js" }]
}
